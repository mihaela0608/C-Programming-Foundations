#include <stdio.h>

int main(){
    // Файловете са потоци от данни
    // FILE* fp = fopen("path", "r") -> получава 2 параметъра, път -> url до целия файл, режима -> read(r), write(w)!!! изтрий файла, append(a) и т.н.
    // в бинарни имат b -> rb, wb, ab
    // правим винаги проверка 
    // if(fp == NULL){
    // printf(); exit(1);
    //}
    // Задължително трябва да използваме функцията fclose(fp); 
    // fprintf(fp, "%..." a) 
    // fscanf(fp, "%..." &a)
    // fgets(&(масив), num(бройка), fp)
    // fread(&, type, num, fp)
    // fwrite(&, type, num, fp)
    
}